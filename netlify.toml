[build]
  command   = "jekyll build"
  publish   = "_site"

[build.environment]
  JEKYLL_ENV = "production"
  RUBY_VERSION = "3.2.0"

# Redirect bare URLs to clean permalinks (optional but recommended)
[[redirects]]
  from = "/*"
  to   = "/404.html"
  status = 404
